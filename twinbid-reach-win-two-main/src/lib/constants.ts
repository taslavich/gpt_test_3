// Default manager telegram contact assigned to every newly created user.
// This value is sent from the frontend on signup and stored in the profile.
// Admins can later override it manually in the database for specific users.
export const DEFAULT_MANAGER_TELEGRAM = "GregTwinbid";
