apiVersion: v1
kind: Service
metadata:
  name: redis-service-external
  namespace: exchange
  labels:
    app: redis
    exposure: external
spec:
  type: NodePort
  selector:
    app: redis
  ports:
    - name: redis
      protocol: TCP
      port: 6379
      targetPort: 6379
      nodePort: 31379
  externalTrafficPolicy: Cluster
