apiVersion: v1
kind: Service
metadata:
  name: orchestrator-service-external
  namespace: exchange
  labels:
    app: orchestrator
    exposure: external
spec:
  type: NodePort
  selector:
    app: orchestrator
  ports:
    - name: grpc
      protocol: TCP
      port: 8081
      targetPort: 8081
      nodePort: 30081
  externalTrafficPolicy: Cluster
